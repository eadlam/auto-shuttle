/**
 * api module
 * @module api
 */

'use strict';

var _ = require('lodash');
var axios = require('axios')
var Immutable = require('immutable');

var MAP_ENDPOINT = 'https://chezcosa.firebaseio.com/recipes.json';

module.exports = {

  post: function(data){

    var promise = new Promise(function(resolve, reject){

    });
    return promise;
  }
};

